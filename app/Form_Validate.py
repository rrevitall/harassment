from wtforms import Form, BooleanField, TextField, PasswordField, validators
# reportForm

def events(self):
    submitBtn.click.connect(fill_info)
    text1.leftTextBox(validate)

def fill_info(self):
    x1=.value
    x2=.value
    insert to db

def validate(self):
    if....
        xrange
    else
        error statement



